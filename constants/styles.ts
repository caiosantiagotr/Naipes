import { StyleSheet } from "react-native";
// Exportando os estilos diretamente como um objeto
const styles = StyleSheet.create({
    centralizado: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
});

export default styles;
